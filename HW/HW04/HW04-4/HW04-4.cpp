#include <stdio.h>

int main () {

    float h, w ;

    printf( "Enter H & W = " ) ;
    scanf( "%f %f", &h, &w ) ;
    printf( "Triagle Area = %.1f", ( h * w ) / 2 ) ;

    return 0 ;
}//end main